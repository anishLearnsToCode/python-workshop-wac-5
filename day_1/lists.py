"""
List / Array
[]
- iterable
"""

numbers = []
print(type(numbers))
print(len(numbers))

# add numbers

# append(value) --> adds at the end of list
# insert(index, value) --> adds value at particular index

# removing elements from a list
# pop() --> removes at the end
# remove(value) --> removes that value
# del keyword del list_name[index] --> removes value at index
